# Backend - copy to .env and fill in. Never commit .env.
# Production: set these on your host (Heroku, Railway, etc.).

PORT=1331
SECRET=your-jwt-secret-use-openssl-rand-hex-32
MONGODB_URI=mongodb://127.0.0.1:27017/tax_filing

# Production only: frontend origin for CORS (e.g. https://yoursite.com)
# FRONTEND_URL=https://yoursite.com
